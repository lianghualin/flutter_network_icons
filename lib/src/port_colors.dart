import 'package:flutter/material.dart';

/// Color constants for the port icon (flat style only).
/// LNM style reuses LnmIconColors directly.
class PortColors {
  // Link Up — professional green
  static const upStroke = Color(0xFF27AE60);
  static const upFill = Color(0xFFD4EFDF);

  // Link Down — light grey
  static const downStroke = Color(0xFFAAAAAA);
  static const downFill = Color(0xFFE8E8E8);

  // Admin Down — dark grey
  static const disabledStroke = Color(0xFF666666);
  static const disabledFill = Color(0xFFC0C0C0);
}
